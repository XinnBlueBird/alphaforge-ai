import FullTerminal from "@/components/FullTerminal";

export const metadata = {
  title: "Terminal · AlphaForge AI",
  description: "Full-screen multi-agent AI terminal — token alpha, contract audit, bot generation.",
};

export default function TerminalPage() {
  return <FullTerminal />;
}
